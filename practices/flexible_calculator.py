#LM 2nd Flexible Calculator
'''
define function calculate, get OPERATION and NUMBERS
    if OPERATION is sum
        return NUMBERS added together
    else if OPERATION is average
        return NUMBERS added together, divided by number of NUMBERS
    else if OPERATION is max
        return largest number in NUMBERS
    else if OPERATION is min
        return smallest number in NUMBERS
    else if OPERATION is product
        return NUMBERS multiplied together
define function check, get INPUT
    try to make INPUT a float,
    if error, return False
    else, return float INPUT
display enter numbers, done to stap entering numbers
loop while INPUT not done:
    set INPUT to user input
    
'''